var should = require('chai').should()
  , sut = require('../lib/index');

describe('Server', function() {
  
  var suite = this;

  before(function() {
  });
  
  it('should', function() {
  });
  
});